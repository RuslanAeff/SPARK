# SPARK Akademik İzlenebilirlik Kaydı

## 1. Amaç ve kapsam

Bu belge, SPARK'ın geliştirme sürecinde bir gereksinimin hangi karara, kod
değişikliğine ve doğrulama kanıtına bağlandığını göstermeyi amaçlar. Tez veya
diploma çalışmasında kullanılacak iddialar için bir indeks görevi görür; tek
başına test raporu, kullanıcı onayı veya bağımsız doğrulama değildir.

`DESIGN_BRIEF.md` yaşayan ürün ve UX kaynağıdır; teknik mimari
`docs/ARCHITECTURE.md` ve ilgili ADR'lerde tutulur. Geçmiş bir iddia yalnız bu
belgelerden birinde yazdığı için doğrulanmış sayılmaz. Her iddia mümkün olduğunda
sabit bir Git commit'ine ve yeniden üretilebilir kanıta bağlanmalıdır.

## 2. Sorumluluk ve onay ilkesi

- Ürün amacı, kabul kriteri ve nihai karar insana aittir.
- Yapay zekâ analiz, öneri, kod ve test taslağı üretebilir; bunlar açık insan
  onayı veya bağımsız doğrulama olmadan insan kararı olarak sunulamaz.
- “Uygulandı”, “test edildi”, “cihazda doğrulandı” ve “yayımlandı” ayrı
  durumlardır. Biri diğerini ima etmez.
- AI tarafından yazılan kayıt, AI katkısı alanında açıkça belirtilir.
- İnsan onayı için tarih, onayın kapsamı ve mümkünse konuşma/issue/commit
  referansı kaydedilir. Sadece “onaylandı” yazmak yeterli değildir.

## 3. Kanıt düzeyleri

| Kod | Anlam | Asgari kanıt |
|---|---|---|
| `E0` | İddia veya retrospektif anlatı | Kaynak belge ve sınırlama notu |
| `E1` | Depoda gözlemlenen uygulama | Commit hash + dosya/satır veya diff |
| `E2` | Otomatik doğrulama | Commit hash + tam komut + tarihli sonuç/CI bağlantısı |
| `E3` | Hedef cihazda doğrulama | Build kimliği + cihaz/OS + senaryo + sonuç + artefakt |
| `E4` | İnsan kabulü veya sürüm kanıtı | Açık kabul kaydı ve/veya yayımlanmış sürüm kimliği |

Bir kaydın kanıt düzeyi, sahip olduğu en yüksek numara değil, iddiayı gerçekten
destekleyen düzeydir. Örneğin typecheck başarısı görsel flicker'ın düzeldiğini
kanıtlamaz; bu iddia için `E3` gerekir.

## 4. Durum sözlüğü

`Önerildi` → `İnsan tarafından onaylandı` → `Uygulandı` → `Otomatik doğrulandı`
→ `Cihazda doğrulandı` → `Kabul edildi` → `Yayımlandı`

Bir kayıt birden fazla durumu ayrı tarihlerde taşıyabilir. “Retrospektif” etiketi,
olay sırasında tutulmayan ve daha sonra Git/dokümanlardan yeniden kurulan bilgiyi
gösterir.

## 5. SPARK gereksinim–kanıt matrisi

Aşağıdaki başlangıç satırları mevcut Git geçmişi ve `DESIGN_BRIEF.md` üzerinden
retrospektif olarak çıkarılmıştır. Bunlar bağımsız doğrulama değildir; boş kanıt
alanları tamamlanmadan akademik sonuç iddiasında kullanılmamalıdır.

| Kimlik | Gereksinim / problem | Karar veya sözleşme | Uygulama referansı | Doğrulama referansı | İnsan/cihaz kabulü | Düzey | Retrospektif sınırlama |
|---|---|---|---|---|---|---|---|
| `SPK-DOM-001` | Analiz projeksiyonu Dashboard ile aynı bütçe dönemini kullanmalı | Takvim ayı yerine bütçe döngüsü ve `effectiveBudget` esas alınır | Commit `5248b98`; `app/(tabs)/analytics.tsx`, `src/utils/spendingProjection.ts` | `src/utils/__tests__/spendingProjection.test.ts`; tarihli komut çıktısı eklenecek | Cihaz kaydı eklenecek | `E1` | Kayıt olaydan sonra commit ve rehberden çıkarıldı |
| `SPK-DOM-002` | Fiş indirimi ve toplamı küçük kullanıcı düzeltmelerinde binary float artığı üretmemeli; basılı toplam AI kalem farkıyla ezilmemeli | Toplam/indirim minor-unit tamsayılarıyla hesaplanır; DAO yazmaları para hassasiyetine normalize edilir; explicit item mutasyonu ve header senkronu atomiktir. İlk ingestion basılı toplamı korur; eski REAL artıkları başlığı yeniden yorumlamayan tek seferlik migration ile temizlenir | Mevcut çalışma ağacı; `src/utils/{moneyMath,receiptMoney}.ts`, `src/db/{database,expenseDao}.ts`, `src/services/{geminiService,receiptLineMerge,receiptParser}.ts`, `app/{add-expense,edit-items}.tsx` | `AI-2026-08-09-RECEIPT-MONEY-001`: typecheck exit 0; tam Jest 43/43 suite ve 310/310 test; Android Metro export 1.923 modül | Örnek fişte `3,17` indirim, `55,93` toplam, `0,20` değişiklik, yeniden açma ve upgrade migration'ı hedef cihazda bekleniyor | `E0` | Otomatik testler saf hesap ve mock DAO/migration sözleşmesini doğrular; gerçek Expo SQLite upgrade ve ekran kabulü henüz cihazda kaydedilmedi |
| `SPK-DOM-003` | Döngü başlangıç günü değiştiğinde tamamlanmış bütçe dönemlerinin tarihleri ve tutarları geriye dönük değişmemeli | Her bütçe kaydı kendi başlangıç/bitiş ve döngü günü anlık görüntüsünü taşır; gün değişikliği yalnız bütçe kaydedildiğinde atomik uygulanır, açık dönem bir önceki gün kapanır ve yeni dönem o gün başlar | Mevcut çalışma ağacı; ADR-008; `src/db/{schema,database,budgetDao}.ts`, `src/hooks/useBudget.ts`, `src/components/BudgetHistoryCard.tsx`, `app/settings-budget.tsx` | `AI-2026-08-21-IMMUTABLE-BUDGET-PERIODS-001`: odaklı Jest 6/6 suite ve 53/53 test; typecheck sonucu aynı kayıtta tutulur | Upgrade migration'ı ve 23→21 geçişi gerçek cihazda bekleniyor | `E0` | SQLite testleri mock tabanlıdır; fiziksel cihazdaki mevcut verinin migration sonucu ayrıca doğrulanmalıdır |
| `SPK-REL-001` | Temiz kurulumda DB seed ve eşzamanlı okuma çakışmamalı | DB tüketicileri seed tamamlanana kadar ortak init promise'ini bekler | Commit `5865af8`; `src/db/database.ts` | Temiz-kurulum otomasyonu veya logu eklenecek | Cihaz/build kaydı eklenecek | `E1` | Kök neden açıklaması `DESIGN_BRIEF.md` P28'e dayanır |
| `SPK-I18N-001` | AZ/RU çevirileri derlemede kaybolmamalı | Üretilmiş locale dosyaları doğrudan düzenlenmez; `map-*` + derleme + parity testi kullanılır | `src/i18n/compilePartial.mjs`, `src/i18n/buildLocales.mjs` | `src/i18n/__tests__/localeParity.test.ts`; CI bağlantısı eklenecek | İnsan dil kontrolü ayrı kaydedilecek | `E1` | Geçmişteki sessiz kayıp rehberde retrospektif anlatılmıştır |
| `SPK-UX-001` | Soğuk açılışta ve sekmeler arası geçişte tema/yüzey flicker'ı olmamalı; mounted Tarayıcı aktif temadan sapmamalı | Native splash ve JS boot yüzeyi tek gate; uygulama teması React store üzerinden uygulanır; aynı palet React Navigation context'ine taşınır; lazy scene/placeholder opak tema yüzeyidir | Mevcut çalışma ağacı; `app/{_layout,(tabs)/_layout}.tsx`, `app/(tabs)/scanner.tsx`, `src/theme/{themeStore,navigationTheme}.ts` | `AI-2026-08-09-THEME-CONTINUITY-001`: navigation theme, tab scene ve Scanner regresyonları dahil typecheck exit 0; tam Jest 46/46 suite ve 314/314 test; Android Metro export 1.924 modül | Standalone APK'da cold start, light/dark, Dashboard→Tarayıcı doğrudan dokunma, ilk lazy ziyaret, swipe ve runtime tema değişimi video-kare kontrolü zorunlu | `E0` | Otomatik testler React yüzey sözleşmesini doğrular; native PagerView kompozisyonu, sistem penceresi ve tek karelik flicker cihaz kanıtı olmadan “çözüldü” sayılamaz |
| `SPK-NOTIF-001` | Bildirimler güvenli tekli ve toplu silinebilmeli | Swipe yalnız sil aksiyonunu açar; uzun basma seçim modudur; toplu DB mutasyonu atomiktir | Mevcut çalışma ağacı; `app/notifications.tsx`, `src/notifications/storage.ts` | Test dosyaları ve tam sonuç eklenecek | Gerçek cihaz gesture testi eklenecek | `E0` | Bu kayıt mevcut kodu işaretler; commit/CI/cihaz kabulü bekleniyor |
| `SPK-NOTIF-002` | Fiş bildirimi kullanıcı tarafından düzeltilen son satıcıyı göstermeli; okunmamış durum kartı domine etmemeli | `receipt-saved-{expenseId}` kayıtları tek batch DAO sorgusuyla kanonik harcamaya uzlaştırılır; düzenleme sonrası hedefli yenileme yapılır. Satıcı başlığa taşınır; unread durumu ince sınır/ton, güçlü başlık, küçük nokta ve erişilebilir etiketle gösterilir | Mevcut çalışma ağacı; `app/{add-expense,notifications}.tsx`, `src/notifications/{buildNotifications,receiptNotifications}.ts`, `src/db/expenseDao.ts` | `AI-2026-08-02-NOTIFICATION-QUALITY-001`: odaklı 4/4 suite ve 31/31 test; typecheck; tam Jest 38/38 suite ve 249/249 test; Android export 1.919 modül | Düzenlemeden önce kaydedilen fişte son satıcı, eski feed'in focus-sync onarımı, açık/koyu unread/read, detay, swipe ve seçim modu cihaz kontrolü bekleniyor | `E0` | Bildirim/DAO testleri mock kullanır; gerçek SQLite yaşam döngüsü ve cihaz görsel kabulü commit/build kimliğiyle henüz kanıtlanmadı |
| `SPK-DOC-001` | Proje bilgisi yaşayan rehber, tarihçe ve tez kanıtı olarak ayrılmalı | Tek-kaynak sahipliği; ürün, mimari, ADR, tarihçe ve evidence belgeleri farklı sorumluluk taşır | Mevcut çalışma ağacı; `DESIGN_BRIEF.md`, `AGENTS.md`, `docs/` | `AI-2026-08-01-DOCS-001`: legacy SHA-256, 20 Markdown dosyasında 0 kırık göreli bağlantı, temiz `git diff --check`, typecheck ve 30/30 Jest suite | İnsan belge incelemesi bekleniyor | `E0` | Doğrulama kirli ve commit'e bağlanmamış çalışma ağacında yapıldı; ürün testleri belge içeriğinin akademik doğruluğunu kanıtlamaz |
| `SPK-ANA-001` | Analiz kartları kayıt varken yeni takvim ayında sıfır görünmemeli | Aylık Analiz, Dashboard ile aynı çözülmüş bütçe döngüsünü; karşılaştırma da önceki bütçe döngüsünü kullanır. Geç tamamlanan eski sorgu yeni dönem sonucunu ezemez | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/utils/analyticsPeriod.ts`, `src/hooks/useExpenses.ts`, çapraz-ay heatmap bileşenleri | `AI-2026-08-01-ANALYTICS-001`: `npm run typecheck` exit 0; tam Jest 32/32 suite, 207/207 test; Android Metro export başarılı | Hedef cihazda 23 Tem–22 Ağu senaryosu bekleniyor | `E0` | Kod ve yerel test henüz commit/build kimliğine bağlı değildir; 526,82 tutarı cihaz DB'sinde bu oturumda doğrudan okunmadı |
| `SPK-UX-002` | Ürün fiyat grafiğinde seçilen değer diğer gözlemleri örtmemeli ve öngörülebilir biçimde kapatılabilmeli | Kayan tooltip yerine grafik dışında sabit inceleme şeridi; çakışmayan seçim bantları; yeniden dokunma, açık kapatma ve bağlam değişiminde sıfırlama; hassas para gösterimi | Mevcut çalışma ağacı; `src/components/LineChart.tsx`, `src/components/ItemAnalyticsModal.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-01-PRICE-CHART-001`: `npm run typecheck` exit 0; tam Jest 33/33 suite, 213/213 test; Android Metro export başarılı | Hedef cihazda yoğun nokta, sheet scroll, açık/koyu tema ve TalkBack kontrolü bekleniyor | `E0` | Kullanıcı ekran görüntüleri başlangıç durumunu gösterir; otomatik test gerçek cihaz gesture ve görsel yerleşimini tek başına kanıtlamaz |
| `SPK-ANA-002` | Yıllık analiz, döneme uymayan kartla veya aynı satıcının tekrarlarıyla bilgi değerini düşürmemeli | Yıllıkta ay sonu projeksiyonu render edilmez; en yüksek işlemler her satıcının en yüksek tek gerçek işlemi olarak DAO'da seçilir ve kart bu kuralı açık başlık/açıklamayla belirtir. Diğer dönemlerde genel işlem sıralaması korunur | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/db/expenseDao.ts`, `src/hooks/useExpenses.ts`, `src/components/analytics/{ProjectionCard,TopTxCard}.tsx` | `AI-2026-08-02-ANALYTICS-DENSITY-001`: typecheck exit 0; tam Jest 37/37 suite, 236/236 test; Android export 1.918 modül | Aynı yıllık aralıkta tekrarlı kira ve farklı satıcılarla yıl/ay geçişi; kart sırası ve açık/koyu tema kontrolü bekleniyor | `E0` | DAO testi SQL sözleşmesini mock ile doğrular; gerçek cihaz SQLite sonucu ve uzun başlık yerleşimi insan kabulü bekler |
| `SPK-UX-003` | Uzun ürün fiyat geçmişi, grafiği nokta kalabalığına dönüştürmeden finansal gerçeği korumalı | Ardışık aynı fiyat/satıcı koşuları sınırlarıyla sıkıştırılır; gerekirse ilk/son, kaynak konumu ve gerçek kova uçları korunarak en fazla 32 gerçek gözlem çizilir; görsel işaretler 12'ye seyreltilir, tek geniş yüzey en yakın gözlemi seçer, ham geçmiş erişilebilir kalır ve sadeleştirme sayıları açıklanır | Mevcut çalışma ağacı; `src/utils/priceHistorySeries.ts`, `src/components/{ItemAnalyticsModal,LineChart}.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-02-ANALYTICS-DENSITY-001`: saf seri ve component testleri dahil tam Jest 37/37 suite, 236/236 test; typecheck ve Android export başarılı | 2, 23, 32 ve 100+ kayıtlı ürünlerde seçim, sheet scroll, büyük font ve TalkBack kontrolü bekleniyor | `E0` | X ekseni gerçek gün aralıkları yerine kronolojik gözlem sırasını sürdürür; otomatik test gerçek cihaz jest ve görsel yoğunluğu tek başına kanıtlamaz |
| `SPK-UX-004` | Kayıtlı hedef yokken silme eylemi sunulmamalı; hedef silme kategori limitlerini kaybettirmemeli | Silme görünürlüğü form metnine değil kalıcı hedef varlığına bağlıdır; DAO gerçek satır değişimini bildirir; boş/stale durumda uyarı verilir ve hedef ile limit yaşam döngüleri ayrılır | Mevcut çalışma ağacı; `app/goal-settings.tsx`, `src/db/{goalDao,categoryLimitDao}.ts`, dört dil kaynağı | `AI-2026-08-09-GOAL-DELETE-001`: typecheck exit 0; hedef/locale odaklı 3/3 suite ve 16/16 test; tam Jest 48/48 suite ve 319/319 test | Hedef yok, hedef+limit var ve hızlı çift dokunma senaryoları standalone cihazda bekleniyor | `E0` | Component testleri React durum/DAO sözleşmesini mock'lar; gerçek SQLite ve modal dokunma davranışı cihaz kabulü bekler |
| `SPK-UX-005` | Birikim hedefi isteğe bağlı olarak daha görünür olmalı; açık borç önceliğini bozmamalı, aynı hedefi tekrarlamamalı ve limitleri hedefe bağlamamalı | Varsayılan kapalı yerel tercih yalnız aktif/tamamlanmamış hedefi borç uyarısından sonra kompakt gösterir; tam kart aynı anda gizlenir, tamamlanan hedef standart karta döner. Hedef yüzeyleri yalnız ilk kalıcı tercih yüklemesinde bekler; sonraki yenilemeler mevcut kartı korur. Hesap ve katkı sheet'i iki görünümde ortaktır; haptic hatası tamamlanmış finansal yazmayı geri çeviremez | Mevcut çalışma ağacı; `app/{(tabs)/index,settings-budget}.tsx`, `src/components/SavingsGoal{Card,PulseCard,ContributionSheet}.tsx`, `src/services/goalFeatureSettings.ts`, `src/utils/{savingsGoalProgress,dashboardGoalPresentation}.ts`, dört dil kaynağı | `AI-2026-08-09-GOAL-FOCUS-001`: odaklı 8/8 suite ve 41/41 test; typecheck exit 0; tam Jest 55/55 suite ve 349/349 test; final Android Metro export 1.928 modül; `git diff --check` temiz | Standalone APK'da açık/koyu tema, dört dil, büyük font, borç+hedef önceliği, tercih kalıcılığı, aktif/tamamlanmış/gecikmiş hedef ve hızlı katkı bekleniyor | `E0` | Testler sunum/tercih/hesap sözleşmesini doğrular; gerçek SQLite, font ölçekleme, haptic ve fiziksel dokunma yerleşimi cihaz kabulü bekler |
| `SPK-UX-006` | Fiş tarama giriş ekranı eski/ağır ikon ve kartlarla amatör görünmemeli; kamera/galeri kaynak seçimi tema, erişilebilirlik ve hızlı dokunmada güvenilir kalmalı | Sol hizalı sakin bilgi hiyerarşisi; platform ikonundan bağımsız özel belge-tarama vektörü; ortak geometride tema yüzeyli kamera/galeri rayları ve aktif paletin farklı vurgu ağırlıkları; sonuç Kaydet CTA'sından ayrı kaynak-seçici semantiği. Picker ve sonuç yazımı ref tabanlı same-frame guard ile tekilleştirilir | Mevcut çalışma ağacı; `app/(tabs)/scanner.tsx`, `src/components/__tests__/ScannerScreen.test.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-13-SCANNER-VISUAL-REFINEMENT-001`: typecheck exit 0; Scanner odaklı 1/1 suite ve 3/3 test; tam Jest 71/71 suite ve 619/619 test; `git diff --check` exit 0 | Standalone/Expo development build'de açık-koyu tema, küçük ekran, büyük font, TR/EN/AZ/RU, TalkBack, izin reddi, kamera/galeri açma-iptal ve hızlı çift dokunma bekleniyor | `E0` | Component testi picker ve tema yüzeyini mock'lar; fiziksel sistem picker'ı, optik kalite ve cihaz font ölçeği insan kabulü olmadan kanıtlanmaz |
| `SPK-UX-007` | Kullanıcı açık/koyu görünümü bozmadan SPARK'ı kontrollü bir vurgu rengiyle kişiselleştirebilmeli; seçim ilk karede flicker, navigator remount, düşük CTA kontrastı, semantik renk kaybı veya merkezden kaçan/kararsız bir carousel üretmemeli | ADR-007: görünüm ve beş küratörlü vurgu ayrı eksenlerdir; şema+vurgu DB readiness içinde atomik snapshot olur. Tam palet/revision runtime reaktivitesi ile Navigation context'i remount olmadan güncellenir; dolu CTA `primaryAction`/`onPrimary` kullanır. Görünüm kartı yerleşik otomatik anahtarı ve koşullu Açık/Koyu kontrollerini korur. Vurgu carousel'i gerçek viewport'a göre adaptif merkezlenir; kullanıcının geçtiği her yeni kademeyi hedef Samsung kabulüne göre Android'de tok OEM `CONTEXT_CLICK`, iOS'ta `RIGID` impact ve bas/ikinci mandal/uzun rezonans içermeyen özgün 12 ms tiz yerel “tik” sesiyle 100 ms ritimde önizler; yaklaşık 96 dp yuva ve kuvvetli fren hızlı savrulmayı sınırlar ve DB'ye yalnız nihai snap bir kez yazılır. Programatik hizalama sessizdir; ses/haptic hatası seçimi engellemez. Semantik, kategori/grafik, logo/splash renkleri sabittir; mikrofon/arka plan ses yeteneği açılmaz ve cihaz-yerel vurgu backup v4 gerektirmez | Mevcut çalışma ağacı; `src/theme/{colors,themeStore,navigationTheme,susevar}.ts`, `src/utils/themeSchedule.ts`, `src/components/{AutoThemeScheduleToggle,AccentPaletteCarousel}.tsx`, `assets/audio/palette-detent.wav`, `scripts/generate-palette-detent-audio.cjs`, `app/{_layout,settings-general}.tsx`, `app.json`, `package.json`, tema tüketicileri ve ADR-007 | `AI-2026-08-14-ACCENT-DETENT-001`: `npm run typecheck` exit 0; odaklı Jest 5/5 suite ve 40/40 test; tam Jest 77/77 suite ve 665/665 test; Android Metro export 1.956 modülle tamamlandı ve WAV varlığı pakete dahil edildi; Expo yapılandırma introspection'ı başarılı; `git diff --check` exit 0 | Standalone APK'da beş vurgu × açık/koyu görünüm, cold start ve runtime kalıcılığa ek olarak ilk/orta/son merkezleme, yavaş/hızlı swipe, kademe başına tek `CONTEXT_CLICK`+12 ms tiz “tik”, 100 ms ritim/96 dp yuva/fren, sessiz/titreşim/medya sesi, TalkBack ve geri bildirim hatasında seçim devamlılığı bekleniyor | `E0` | Çalışma ağacı henüz commit/build kimliğine bağlı değildir; Jest native ses/haptic zamanlamasını, OEM motor hissini, sistem sessizliğini, carousel optik kalitesini veya ilk kareyi kanıtlamaz |
| `SPK-UX-008` | Kategori sınırları hedefe bağlı veya gizli görünmemeli; fiyat takibi yalnız otomatik ilk altıyla sınırlanmamalı; analiz başlıkları ve yön işaretleri optik olarak hizalı, projeksiyon bütçe eşiği görünür olmalı | Hedefsiz limit kaydı desteklenir; Bütçe ve boş Analiz kartı yönetim girişine bağlanır ve takvim ayı kapsamını açıklar. Fiyat kartı tüm gerçek değişimleri sabit altılı yatay sayfalara böler. Etkilenen kart başlıkları ortak ölçü, donut yönleri aynı vektör ikon ailesini kullanır. Projeksiyon bütçe işaretçisi dolgudan üstte, kontrast kenarlı ve uçlarda içeri kısılmıştır | Mevcut çalışma ağacı; `app/{goal-settings,settings-budget,(tabs)/analytics}.tsx`, `src/components/analytics/{analyticsStyles,LimitsHealthCard,PriceWatchCard,ProjectionCard,DonutCard ve ilgili kartlar}.tsx`, dört dil kaynağı | `AI-2026-08-21-ANALYTICS-AFFORDANCE-001`: odaklı test ve typecheck sonucu katkı kaydında | Android açık/koyu tema, büyük yazı, hedef olmadan limit kaydı, 7+/13+ fiyat değişiminde yatay sayfalama, bütçe eşiğinin %1/%99 konumu ve donut okları bekleniyor | `E0` | Component testleri gerçek nested gesture, font ölçeği ve görsel optik kabulü kanıtlamaz |
| `SPK-UX-009` | Kalıcı silme onayı işlemin ciddiyetini açıklarken kullanıcıyı yoğun alarm görselleriyle baskılamamalı ve tema bağlamından kopmamalı | Ortak silme modalı tema yüzeyi, tek çöp kutusu ikonu, normal başlık hiyerarşisi, açık geri alınamazlık metni ve 48 dp eylemler kullanır; yalnız Sil düğmesi kontrastlı koyu kırmızıdır. HUD köşeleri, scanline, ayırıcı, kırmızı dış parlama ve büyük tekrar başlık kaldırılır. İşlem toplu silmede tekil/çoğul başlık ve açıklama ayrılır | Mevcut çalışma ağacı; `src/components/GlassDeleteModal.tsx`, `app/(tabs)/transactions.tsx`, dört dil kaynağı ve `src/components/__tests__/GlassDeleteModal.test.tsx` | `AI-2026-08-22-DELETE-CONFIRM-REDESIGN-001`: typecheck başarılı; tam Jest 93/93 suite ve 744/744 test; locale parity geçti | Expo Go/standalone Android'de tekli/çoklu işlem, backdrop/geri/İptal/Sil, açık-koyu tema, büyük font ve TR/EN/AZ/RU optik kontrolü bekleniyor | `E0` | Component testi eylem ayrımını ve metni doğrular; gerçek native Modal gölgesi, font ölçeği ve görsel insan kabulü cihazda değerlendirilmelidir |
| `SPK-UX-010` | Düzenli ödeme yönetimi veri/yedekleme işi gibi sunulmamalı; aynı açıklama ayar kartında ve bilgi penceresinde tekrarlanmamalı | Ayar grubu “Bütçe ve planlama” olarak adlandırılır; “Düzenli ödemeler” girişi buraya taşınır. Veri ve yedek yalnız satıcı, yedekleme ve geri yükleme kapsamını açıklar. Hedefi öne çıkar ve bu ay kategori limitleri satırları yalnız başlık+kontrol/ok kullanır; ayrıntılı açıklamalar mevcut bilgi/ayrıntı yüzeylerinde kalır | Mevcut çalışma ağacı; `app/{settings-budget,settings-data,subscriptions}.tsx`, `app/(tabs)/settings.tsx`, dört dil kaynağı ve `src/components/__tests__/SettingsBudgetScreen.test.tsx` | `AI-2026-08-22-SETTINGS-PLANNING-IA-001`: typecheck başarılı; odaklı Jest 2/2 suite ve 15/15 test; tam Jest 93/93 suite ve 745/745 test; locale parity geçti | Android açık-koyu tema, büyük font, dört dil; Ayarlar→Bütçe ve planlama→Düzenli ödemeler; Veri ve yedek içinde bağlantının bulunmaması; iki kompakt satırın optik kontrolü bekleniyor | `E0` | Component testi rota ve metin yokluğunu doğrular; gerçek ekran yüksekliği, grup açıklaması kesilmesi ve görsel yoğunluk cihaz kabulü ister |
| `SPK-DOM-003` | Gramajlı/hacimli fiş kalemleri adet gibi görünmemeli; fiyat takibi uyumsuz ölçüleri veya aynı gün tekrarlarını sahte değişim olarak birleştirmemeli | Kalıcı `piece/kg/l` ölçü alanı; g/ml girişinin kanonik dönüşümü; AI, manuel edit, migration ve backup entegrasyonu; normalize ad+ölçü grubu, aynı gün ağırlıklı fiyat ve iki ayrı gün şartı; kart dokunuşunda üst sekme swipe kilidi | Mevcut çalışma ağacı; ADR-009; `src/utils/{measurementUnit,priceWatch}.ts`, fiş/DB/backup servisleri, `app/{edit-items,(tabs)/analytics}.tsx`, fiyat kartı ve ürün modalı | `AI-2026-08-21-MEASUREMENT-PRICE-001`: typecheck ve odaklı otomatik test sonucu katkı kaydında | Eski DB upgrade, 530 g AI/manüel giriş, fiyat kartında yatay swipe ile sekme ayrımı ve APK cihaz kontrolü bekleniyor | `E0` | Kesirli eski miktar migration'ı kontrollü heuristiktir; gerçek PagerView arbitration ve SQLite upgrade yalnız cihaz/build kabulüyle kesinleşir |
| `SPK-DOM-004` | Aynı fiziksel ürünün güvenli yazım/OCR/birim eki farkları fiyat geçmişini bölmemeli; farklı ölçü, paket, marka ve varyantlar sessizce birleşmemeli | ADR-010: birim-duyarlı deterministik canonicalization; taşınabilir UID'li `canonical_products`, tekil ölçü-duyarlı aliaslar ve nullable kalem bağlantısı; ham ad/çeviri ile ayrı kullanıcı etiketi; AI yalnız sınırlı metadata veya açık kullanıcı önerisi, nihai merge değil; kullanıcı merge/split finansal gözlemi korur; backup v4 ve v1-v3 import uyumluluğu | Mevcut çalışma ağacı; `src/utils/{productIdentity,itemDisplayName,priceWatch}.ts`, `src/db/{schema,database,productIdentityDao,expenseDao}.ts`, `src/services/{geminiService,receiptParser,backupService}.ts`, `app/product-matching.tsx`, `app/{_layout,settings-data}.tsx` ve ilgili analiz tüketicileri | `AI-2026-08-23-CANONICAL-PRODUCT-IDENTITY-001`: typecheck exit 0; odaklı 11/11 suite ve 141/141 test; tam Jest 98/98 suite ve 813/813 test; `git diff --check` temiz | Eski gerçek DB upgrade'i, v3→güncel ve v4 tekrar restore, AI fiş önizlemesi, kullanıcı etiketi, merge/split ve Fiyat Takibi cihaz kontrolü bekleniyor | `E0` | Çalışma ağacı henüz commit/build kimliğine bağlı değildir; otomatik testler gerçek Expo SQLite migration'ı, Gemini doğruluğunu veya kullanıcı merge/split kabulünü kanıtlamaz |
| `SPK-REL-002` | TR dışındaki dilde fiş taraması çevrilmemiş/kategorisiz/sıfır kayıt üretmemeli; kamera dönüşü veya ağ işi sınırsız askıda kalmamalı | Seçili TR/EN/AZ/RU dili yapılandırılmış `localized_name` sözleşmesine taşınır; kategori dil bağımsız `category_key` üzerinden yerel DB adına çözülür. Satıcı+tarih+kalem+tutar kalite kapısı kanıtsız sıfır veya boş taslağı reddeder ve geçersiz ilk model sınırlı fallback'e gider. Görsel en-boy oranlı 2048 px/%82 hazırlanır; görüntü/ağ/toplam timeout, AbortSignal, geç sonuç kimliği ve Android pending-result kurtarma kullanılır. Detaylı düzenleme sıfır taslak oluşturmaz; taranmış/kayıtlı işlem para birimi önizleme, işlem satırı ve kalem düzenleme boyunca korunur | Mevcut çalışma ağacı; `app/{(tabs)/scanner,add-expense,edit-items}.tsx`, `src/services/{geminiService,receiptParser}.ts`, `src/utils/{receiptCategory,imageCompressor}.ts`, `src/components/TransactionRow.tsx`, dört dil kaynağı ve ilgili testler | `AI-2026-08-23-MULTILINGUAL-RECEIPT-RELIABILITY-001`: typecheck exit 0; odaklı 7/7 suite ve 70/70 test; tam Jest 103/103 suite ve 878/878 test; locale parity ve `git diff --check` geçti | Gerçek TR/EN/AZ/RU seçimiyle aynı fiş; kamera/galeri, uygulama Activity yeniden oluşturma, Durdur, bulanık/uzun fiş, yavaş/kotasız ağ ve PLN/USD/AZN/TRY gösterimi fiziksel cihazda bekleniyor | `E0` | Jest kamera, ImageManipulator, SecureStore, Gemini ve ağ katmanlarını mock'lar; gerçek OCR/çeviri doğruluğu, OEM Activity yaşam döngüsü, bellek ve timeout hissi cihaz kanıtı olmadan kesinleşmez |
| `SPK-UX-011` | Benzer ürün kataloğu veri arttığında sonsuz bir kart akışına dönüşmemeli; kullanıcı olası eşleri ve eski bir ürünü bulabilmeli | Varsayılan yerel olası-eşleşme inceleme kuyruğu yalnız öneridir, otomatik merge/ekran açılışında AI çağrısı yoktur. Tüm ürünler sanallaştırılmış `SectionList` içinde 0–30/31–90/91–365/>365/geçmiş yok bölümlerine ayrılır; çok alanlı arama, ölçü+tarih filtreleri ve son görülme/sıklık/ad sıralaması vardır. İlk seçim ikinci adayı aynı ölçüyle sınırlar. DAO alias ve gözlemi ayrı CTE'lerde agrega ederek satır çoğalmasını önler | Mevcut çalışma ağacı; ADR-010; `app/product-matching.tsx`, `src/utils/productMatchDiscovery.ts`, `src/db/productIdentityDao.ts`, dört dil kaynağı | `AI-2026-08-23-PRODUCT-MATCHING-DISCOVERY-001`: typecheck exit 0; odaklı 4/4 suite ve 53/53 test; tam Jest 99/99 suite ve 843/843 test; `git diff --check` temiz | Fiziksel cihazda yüzlerce/binlerce ürün, açılış/kaydırma/filtre/bellek, büyük font, dört dil ve aynı-birim ikinci seçim doğrulaması bekleniyor | `E0` | Saf testte 2.000 ürün adayı denenmiştir; bu sonuç gerçek Expo SQLite sorgu süresini, React Native çizim akıcılığını veya bellek baskısını kanıtlamaz. Özet sorgusu hâlâ kompakt ürün kataloğunun tamamını belleğe alır; SQL pagination gereksinimi cihaz profiliyle belirlenir |
| `SPK-UX-012` | Dashboard aynı aylık bütçe gerçeğini hem ana bütçe kartında hem sayfa sonundaki üçlü istatistik kartında tekrarlamamalı | Donut altındaki ayrıntılı Bütçe kartı tek bütçe özeti olarak korunur; sayfa sonundaki Aylık bütçe/Kullanılan/Kalan gün kartı ve yalnız ona ait stiller kaldırılır | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-24-DASHBOARD-BUDGET-DENSITY-001`: typecheck ve diff kontrolü katkı kaydında | Android açık/koyu tema, bütçeli/bütçesiz ve geçmiş dönem görünümünde sayfa sonu boşluğu cihazda kontrol edilmeli | `E0` | Otomatik kontrol kaldırılan kartın optik sayfa ritmini veya gerçek cihaz scroll sonunu kanıtlamaz |
| `SPK-UX-013` | Dashboard Sık gidilen yerlerde makul uzunluktaki satıcı adı gereksiz kesilmemeli; gerçek taşma kart sınırını bozmadan okunabilmeli | İki sütun korunur, sütun boşluğu küçültülür ve yüzde adın altına alınarak metin viewport'u genişletilir. Ad yalnız ölçülen doğal genişliği viewport'u aştığında 1,4 saniye sonra 28 px/sn hızla sağdan sola kayar; sığıyorsa animasyon/pil maliyeti üretmez ve erişilebilir tam ad korunur | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, mevcut `src/components/MarqueeText.tsx`, `src/components/__tests__/MarqueeText.test.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-24-DASHBOARD-VENDOR-NAMES-001`: odaklı 1/1 suite ve 2/2 test ile typecheck geçti | Android'de kısa/orta/uzun ad, 0/100 yüzdesi, açık-koyu tema, büyük font ve dört dil optik kontrolü bekleniyor | `E0` | Jest ölçüm eşiği ve negatif yöndeki animasyon hedefini doğrular; gerçek font metriği, UI-thread akıcılığı ve kart optiği cihaz kabulü gerektirir |
| `SPK-UX-014` | Dashboard borç/ek gelir kutuları veri yokken anlamsız çizgi göstermemeli; veri varken tutarın finansal kapsamı anlaşılmalı | Ortak iki-kutu bileşeni boş borcu “Açık borç yok”, boş dönem gelirini “Bu dönem ek gelir yok” olarak sunar. Borç varsa global açık bakiye; ek gelir varsa seçili dönemde bütçeye eklenen yeşil `+tutar` gösterilir. Görsel durum ve bağlam erişilebilir etikete de katılır; mevcut sheet eylemleri korunur | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, `src/components/DashboardCashEntryTiles.tsx`, dört dil kaynağı ve component testi | `AI-2026-08-24-DASHBOARD-CASH-STATUS-001`: typecheck; component+locale parity 2/2 suite ve 14/14 test; `git diff --check` sonucu katkı kaydında | Android'de boş/yalnız borç/yalnız gelir/ikisi dolu, geçmiş dönem, büyük tutar, açık-koyu tema, büyük font ve dört dil bekleniyor | `E0` | Test sunum dalları ve eylemleri doğrular; gerçek IncomeSheet→SQLite→refresh zinciri ve optik satır yüksekliği cihaz kabulü gerektirir |
| `SPK-UX-015` | Dashboard donutu kategori toplamını bütçenin tamamı gibi göstermemeli; küçük kategori dilimleri tasarımı bozulan yapay büyütme olmadan seçilebilmeli | Normal halka etkin bütçeyi %100 payda, renkli kategori toplamını kullanılan bölüm ve nötr rayı kalan bölüm yapar; merkez gerçek kullanım oranını gösterir. Kategori dokunuşu aynı segmentleri eski 360° dağılıma büyütür. 44 dp önceki/sonraki kontrolleri her dilime kesin erişim sağlar; ayrıntı harcamadaki payı bütçedeki paydan ayırır. Dinamik yay boşluğu küçük segmenti tamamen silmez. İnce iç/dış cam kenarı halkanın sınırını tanımlar; merkezde dekoratif dışa-genişlet rozeti ve odakta içe-topla ikonu aynı eylem dilini kurar | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, `src/components/{DashboardBudgetDonut,DonutChart}.tsx`, `src/utils/donutGeometry.ts`, dört dil kaynak/çıktıları ve odaklı testler | `AI-2026-08-24-DASHBOARD-BUDGET-DONUT-001`: typecheck; cam kenarı/affordance sonrası odaklı 2/2 suite ve 9/9 test; final tam Jest 107/107 suite ve 892/892 test; `git diff --check` temiz | Android'de %0/%1/%6/%100/%100+ kullanım; 1–10 kategori; doğrudan dilim/merkez/önceki-sonraki; dönem değişimi; bütçesiz görünüm; iç/dış cam kenarı ve genişlet rozetinin açık-koyu tema görünürlüğü; dört dil, büyük font ve TalkBack bekleniyor | `E0` | Jest payda, metin, prop ve seçim dallarını doğrular; SVG cam kenarının optik yoğunluğu, gerçek parmak isabeti, geçiş akıcılığı ve font sarması cihaz kabulü gerektirir |
| `SPK-UX-016` | Dashboard Üst kategoriler ikonları kategori anlamını kullanıcı tahminine bırakmamalı | Kompakt yatay kartta ikon→yerelleştirilmiş kategori adı→renkli oran hiyerarşisi kullanılır. Ad iki satırla sınırlanır fakat tek satırlık adda boş ikinci satır yüksekliği ayrılmaz; oran gerçek metnin 2 dp altında kalır. Tam ad ve oran tek ekran okuyucu etiketinde korunur | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, `src/components/CategoryPill.tsx`, `src/components/__tests__/CategoryPill.test.tsx`, `DESIGN_BRIEF.md` | `AI-2026-08-24-DASHBOARD-CATEGORY-LABELS-001`: typecheck ve 1/1 suite, 3/3 odaklı test geçti; tam Jest ve diff sonucu katkı kaydında | Android açık-koyu tema, 1–6 kategori, uzun TR/EN/AZ/RU adları, büyük font, yatay kaydırma ve TalkBack bekleniyor | `E0` | Component testi metin sırasını, iki satır sınırını, boş satır ayrılmamasını ve erişilebilir etiketi doğrular; gerçek font ölçüsü ve optik kart yüksekliği cihaz kabulü gerektirir |
| `SPK-UX-017` | Gelir gününe bağlı Dashboard bütçe döngüsü takvim ayı veya “bu ay” olarak yanlış adlandırılmamalı; harcanan tutar aynı ekranda yinelenmemeli | Ay adı yerine küçük “Bütçe dönemi” bağlamı ve kanonik gerçek tarih aralığı gezinme kimliği olur. Aynı ay/yıl kompakt, yıl geçişi iki yıllı biçimlenir. Üstteki tekrar harcama etiketi+tutar kaldırılır; dönem kimliği doğrudan kullanım donutuna, oradan kesin harcanan/kalan tutarı veren bütçe kartına akar | Mevcut çalışma ağacı; `app/(tabs)/index.tsx`, `src/utils/dateUtils.ts`, dört dil kaynak/çıktıları, `src/utils/__tests__/dateUtils.test.ts`, `DESIGN_BRIEF.md` | `AI-2026-08-24-DASHBOARD-PERIOD-IDENTITY-001`: typecheck; tarih+locale odaklı 2/2 suite ve 27/27 test geçti; tam Jest ve diff sonucu katkı kaydında | Android'de ayın 1'i ve 2–31 başlangıçları, geçmiş/güncel dönem, donut normal/kategori odağı, Ara–Oca geçişi, dört dil, büyük font ve ok gezinmesi bekleniyor | `E0` | Saf test tarih biçimi ve locale paritesini doğrular; gerçek cihazda uzun Rusça başlık ölçeği, donut odağı ve optik dikey ritim insan kabulü gerektirir |
| `SPK-UX-018` | Birikim hedefi hızlı eylemi, kullanıcı tıklamadan önce açacağı artırma/azaltma seçeneklerini anlaşılır biçimde haber vermeli | Çelişkili “ekle/çıkar” başlığı ve tek yönlü `+` yerine “Birikimi güncelle” ana eylemi, görünen “Tutar ekle veya azalt” açıklaması ve `±` ikonu kullanılır. Panel aynı ana adı taşır; kompakt kart ve ekran okuyucu etiketi/ipucu aynı sözleşmeyi paylaşır. Ayarlardaki eski “+ düğmesi” açıklaması da yenilenir | Mevcut çalışma ağacı; `src/components/SavingsGoal{Card,PulseCard,ContributionSheet}.tsx`, dört dil kaynak/çıktıları ve ilgili component testleri, `DESIGN_BRIEF.md` | `AI-2026-08-25-SAVINGS-UPDATE-AFFORDANCE-001`: typecheck; hedef kartları+sheet+locale odaklı 4/4 suite ve 17/17 test geçti; tam Jest ve diff sonucu katkı kaydında | Android'de standart/öne çıkarılmış hedef, panel açılışı, ekle/azalt, açık-koyu tema, dört dil, büyük font ve TalkBack bekleniyor | `E0` | Component testleri metin, ikon, erişilebilirlik ve panel açılışını doğrular; gerçek CTA yüksekliği, haptic ve sheet optiği cihaz kabulü gerektirir |
| `SPK-UX-019` | Ayarlar ana menüsü ve alt sayfaları Dashboard/Analiz gibi kalıcı kart yığınına dönüşmemeli; bölüm, gezinme ve gerçek kontrol sınırları açıkça ayrılmalı | Ana menüdeki Genel, Bütçe ve planlama, Veri ve yedek ve Yapay zekâ kartları kaldırılır; renkli ikon, başlık, sarmalanan açıklama ve ok doğrudan sayfa zeminindeki tam genişlikte ayırıcılı satırlarda korunur. Alt sayfalarda dış kart yüzeyleri kullanılmaz; giriş, çip, tarih, anahtar ve CTA sınırları korunur. Bütçe geçmişi ayırıcılı yatay şerit, son yedek sonucu kenar vurgulu özel durum satırıdır; SPARK kimliği ana menüden boşlukla ayrılır | Mevcut çalışma ağacı; `app/(tabs)/settings.tsx`, `app/settings-{general,budget,data,ai}.tsx`, `src/components/{SettingsList,BackupSection,BudgetHistoryCard}.tsx`, `src/components/__tests__/{SettingsScreen,SettingsList}.test.tsx`, `DESIGN_BRIEF.md`, `docs/ARCHITECTURE.md` | `AI-2026-08-25-SETTINGS-FLAT-HIERARCHY-001`: typecheck; ana menü+ortak satır odaklı 2/2 suite ve 4/4 test; tam Jest 112/112 suite ve 912/912 test | Android'de ana menü ve dört alt sayfa; açık/koyu tema, uzun TR/EN/AZ/RU metinleri, büyük font, yatay kontroller, klavye ve TalkBack optik/işlevsel kabulü bekleniyor | `E0` | Otomatik test ana satırlarda kalıcı kart stilinin olmadığını, ayırıcıları, açıklama sarmasını, rota+haptic davranışını ve ortak satır sözleşmesini doğrular; gerçek cihazda optik ayırıcı yoğunluğu, font sarması ve dokunma geri bildirimi insan kabulü gerektirir |
| `SPK-UX-020` | Uygulama içindeki SPARK adı zayıf/statik kalmamalı; aralıkları ve Dashboard sol hizası bozulmadan daha güçlü ve dokunmaya tepki veren AI kimliği vermeli; pil davranışını veya sabit native marka varlıklarını etkilememeli | “SPARK Yaşayan Çekirdek İmzası” ortak SVG/Reanimated bileşenidir. Beş tok harf hero görünümde `32` boyut/`1,2` kontur/`30` merkez adımı; kompakt görünümde `29`/`1,2`/`27` ile sıkı ve eşit yerleşir. Dört vektör noktadan `S` ve `P` sonrasındaki ilk ikisi gerçek glyph yan boşlukları için ölçülü sola düzeltilir, son ikisi geometrik ortayı korur. Daraltılmış canvas görünür S'yi Dashboard içerik kenarına taşır. Üç UI-thread saati `6,2`/`3,8`/`5,4` saniyelik döngülerle iki karşıt yatay akış, yarım fazlı iki merkez dalgası ve iki dolaşan sis çekirdeği olmak üzere altı görünür ortam primitive'i sürer; dokunmasız durumda imza durağan bir ana düşmez. Hareketli katmanların tamamı vurgu paletinin koyu/ana/açık tonlu eliptik radial-gradient'leridir; saf beyaz ve dikdörtgen bant yoktur. Saf tema profili aydınlıkta `S`/`K` temel uçlarını ana vurgu tonuna eşitler, sis yolunu uç harflere genişletip yoğunluğu ölçülü artırır; onaylanan karanlık profil değişmez. İmzanın tek Pressable yüzeyine dokunma, sürekli döngü eklemeden A merkezli kısa parlama ile sola/sağa ayrılan iki organik çekirdeği yeniden başlatır; navigasyon veya veri eylemi yapmaz. Dashboard/Ayarlar aynı bileşeni ve dört dilde erişilebilir ipucunu kullanır. Sekme odağı kaybolduğunda/uygulama arka plana geçtiğinde hareket durur; hareket azaltmada ortam ve dokunma hareketleri statik kalır. Ekran okuyucu tek kontrol okur; uygulama ikonu/splash değişmez; klasik varyant hızlı sunum geri dönüşüdür | Mevcut çalışma ağacı; `src/components/LivingSparkWordmark.tsx`, `app/(tabs)/{index,settings}.tsx`, `src/components/__tests__/LivingSparkWordmark.test.tsx`, dört dil kaynağı, Reanimated test mock'u ve tasarım/kalite belgeleri | `AI-2026-08-25-LIVING-WORDMARK-001`: typecheck; wordmark odaklı 1/1 suite ve 7/7 test; tam Jest 111/111 suite ve 909/909 test geçti; `git diff --check` temiz | Android'de Dashboard/Ayarlar; beş vurgu × açık/koyu; dokunmadan 20 saniyede iki yönlü akış ve iki kademeli dalganın okunması; tok/sıkı geometride kırpılma veya harf teması olmadan genişleyen animasyon yüzeyi; ilk iki noktanın optik yakınlığı, son iki noktanın korunması ve sol hiza; beyaz blok/kareli yüzey/sert bant olmadan çok tonlu organik hareket; aydınlıkta eşit uç tonu ve `S`–`K` boyunca hareket, karanlık görünüm regresyonu; harf/nokta/boşluk dokunuşu ile anlık tepki ve hızlı tekrar; hızlı sekme/arka plan dönüşü; sistem hareket azaltma; büyük font, dört dil, TalkBack ve uzun çalışma/pil-ısı gözlemi bekleniyor | `E0` | Otomatik test tok/sıkı layout ölçülerini, üç ortam döngüsünün hızlarını, altı ortam katmanını, sabit harf merkezlerini, ilk iki noktanın optik düzeltmesini, son iki orta noktayı, aydınlık/karanlık tema profilini, dokunma yeniden başlatmasını, erişilebilir kimliği, locale paritesini ve hareket kapılarını doğrular; gerçek glyph side-bearing'i, SVG kırpılması/radial-gradient banding'i, Reanimated akıcılığı, dokunmasız organik yaşam algısı, gerçek dokunma hissi ve pil etkisi insan cihaz kabulü olmadan kanıtlanmaz |
| `SPK-UX-021` | Veri ve yedek içindeki Satıcı Yönetimi, çok sayıda satıcıyı tek tek silmeye zorlamamalı; toplu silmenin finansal etkisi kullanıcı onayından önce açık olmalı | Açık çoklu seçim modu; seçili sayı, tümünü seç/seçimi temizle/iptal eylemleri; satıcı kutularında checkbox semantiği ve görünür seçim durumu kullanılır. Onay penceresi seçili satıcı ve bağlı işlem sayılarını ayırır. Tekli ve toplu silme aynı `VendorDao.deleteMany` sınırında, bağlı işlemlerle birlikte tek transaction içinde yürütülür; onaydan önce yazma yapılmaz | Mevcut çalışma ağacı; `app/settings-data.tsx`, `src/db/vendorDao.ts`, dört dil kaynak/çıktıları, `src/components/__tests__/SettingsDataVendorSelection.test.tsx`, `src/db/__tests__/vendorDao.test.ts` | `AI-2026-08-25-VENDOR-BULK-DELETE-001`: typecheck; odaklı 4/4 suite ve 19/19 test; tam Jest 114/114 suite ve 919/919 test geçti; `git diff --check` temiz | Android'de 0/1/çok/tüm seçim, arama açıkken seçim, onay iptali, bağlı işlemsiz/bağlı işlemli metin, açık-koyu tema, dört dil, büyük font ve TalkBack; silme sonrası işlem/satıcı/analiz yenilemesi bekleniyor | `E0` | Jest seçim durumunu, toplu etki sorgusunu ve transaction çağrı sırasını doğrular; gerçek SQLite trigger/FK etkisi, yatay seçim optiği ve yıkıcı işlemin cihaz davranışı fiziksel kabul gerektirir |
| `SPK-UX-022` | Analiz sekmesindeki Davranışsal Analiz kartındaki küçük donut dilimleri (ör. `%2` diğer/keyif) doğrudan dokunarak güvenilir seçilemiyor; erişim yalnız isabetli dokunmaya bağlı kalmamalı | `DashboardBudgetDonut`'taki döngüsel önceki/sonraki gezinme deseni `DonutCard`'ın iki bağımsız donut sayfasına (ihtiyaç/keyif, hafta içi/hafta sonu) taşınır. Bir dilim seçiliyken analiz metninin iki yanında 32×32 ok kontrolleri belirir ve seçim dizi boyutuna göre döngüsel ilerler; hiçbir dilim seçili değilken mevcut kaydırma ipucu (sayfalar arası geçiş) değişmeden kalır. Donut'un kendi tıklama/segment seçimi ve sayfa kaydırma davranışı korunur, yalnız ok erişimi eklenir | Mevcut çalışma ağacı; `src/components/analytics/{DonutCard,analyticsStyles}.tsx`, `app/(tabs)/analytics.tsx`, dört dil kaynak/çıktıları, `src/components/analytics/__tests__/DonutCard.test.tsx` | `AI-2026-09-02-DONUT-ARROW-NAV-001`: typecheck; odaklı 3/3 test; tam Jest 115/115 suite ve 945/945 test geçti | Android'de ihtiyaç/keyif ve hafta içi/hafta sonu donut'larında çok küçük (`%1-2`) dilime doğrudan ve ok kontrolleriyle erişim; sayfa kaydırma ile ok dokunmasının çakışmaması; açık/koyu tema, büyük font ve TalkBack; dört dil metin sarması bekleniyor | `E0` | Jest ok basımının doğru yön/segment indeksini tetiklediğini doğrular; gerçek parmak isabeti, buton boyutunun optik yeterliliği ve sayfa kaydırma ile ok dokunması arasındaki gerçek gesture çakışması fiziksel cihaz kabulü gerektirir |
| `SPK-ANA-004` | Ürün fiyat grafiğindeki gözlemler parmakla güvenilir seçilmeli; uzun alım geçmişi modalı uzatmamalı; sınıflandırılmamış harcamalar gerçek bir tasarrufmuş gibi adlandırılmamalı | Her grafik gözlemine 44×44 doğrudan hedef; alım geçmişinde en yeniden eskiye altışarlı yatay sayfalar; nested swipe sırasında üst sekme kilidi; üçüncü davranış segmenti “Diğer Harcamalar” ve dönem harcaması temelli açıklama | Mevcut çalışma ağacı; `src/components/{LineChart,ItemAnalyticsModal}.tsx`, `src/db/expenseDao.ts`, `src/hooks/useExpenses.ts`, dört dil kaynağı | `AI-2026-08-21-PRICE-HISTORY-UX-001`: typecheck, odaklı ve tam Jest sonucu katkı kaydında | APK üzerinde nokta seçimi, modal yatay geçmiş gesture'ı, büyük font ve dört dil optik kontrolü bekleniyor | `E0` | Jest gerçek parmak toleransı, bottom-sheet/native pager gesture yarışını ve optik sonucu kanıtlamaz |
| `SPK-ANA-005` | Davranış kategorileri kullanıcıya açıkça anlatılmalı; günlük dalgalanma uç değerler nedeniyle okunamaz olmamalı; abonelik planı formu ekran/klavye altında kalmamalı ve tekrar aralığı anlaşılır olmalı | Davranış kartına kategori listeli bilgi penceresi; grafikte deterministik tüm dönem/14 gün/7 gün kademeleri, en güncel tarihten geriye hizalanmış yatay pencereler, sağ-uç tarih çıpası, görünür tarih aralığı+sayaç ve pencereye yerel Y ölçeği; zoom veri giriş animasyonunu yeniden başlatmaz. Ödeme planında gerçek tam ekran route, safe-area içinde sabit başlık ve klavyeyle daralan dikey form gövdesi; `-/+` aralık seçici ve “her ay / 2 ayda bir” özeti. İlk `%92` bottom-sheet ile ilk soldan bölünen grafik sayfalama yaklaşımları cihaz ekranlarıyla geçersiz kılındı | Mevcut çalışma ağacı; `app/recurring-payment.tsx`, `src/components/{BarChart,RecurringPaymentReminderSheet}.tsx`, `src/components/analytics/{ChartCard,DonutCard}.tsx`, `src/utils/barChartViewport.ts`, dört dil kaynağı ve ilgili testler | `AI-2026-08-21-ANALYTICS-SUBSCRIPTION-UX-001`, `AI-2026-08-21-RECURRING-PAYMENT-FULLSCREEN-001` ve `AI-2026-08-21-DAILY-FLUCTUATION-VIEWPORT-001`: typecheck; son tam Jest 87/87 suite ve 700/700 test; `git diff --check` temiz | Android APK'da 32 günlük aralıkta tümü → 08.08–21.08 (`3/3`) → 15.08–21.08 (`5/5`), eski sayfada zoom çıpası, hızlı `-/+`, yatay pager gesture'ı, uç değerli veri; ayrıca klavye açıkken create/edit tam ekran form, safe area, büyük font ve dört dil kontrolü bekleniyor | `E0` | Jest gerçek yatay scroll konumunu, optik tarih etiketi yerleşimini ve native klavye/safe-area katmanını kanıtlamaz |
| `SPK-ANA-006` | Çok sayıda satıcı ana Analiz akışını uzatmamalı; satıcı detayı bağlamı kaybetmeden açılmalı; “en çok alınan” ile “en çok harcanan” birbirine karıştırılmamalı | Satıcı özeti sabit yükseklikte beşerli yatay sayfalara ayrılır ve kart içi jest üst sekmeye devredilmez. Gerçek satır dokunuşu ayrı yüksek `VendorAnalyticsSheet` açar; donut, lejant ve tüm gruplanmış ürünler ana karttan ayrılır. Ürünler panel içinde beşerli sayfalanır; varsayılan sıralama alış kaydı sayısıdır ve toplam harcama seçeneği sunulur. Ürün ayrıntısı, satıcı paneli tamamen kapandıktan sonra açılır | Mevcut çalışma ağacı; `src/db/expenseDao.ts`, `app/(tabs)/analytics.tsx`, `src/components/analytics/{VendorsCard,VendorAnalyticsSheet,analyticsStyles}.tsx`, dört dil kaynağı ve ilgili testler | `AI-2026-08-21-VENDOR-PRODUCT-PAGER-001`: typecheck; odaklı 6/6 bileşen testi; tam Jest 88/88 suite ve 708/708 test; locale çıktıları yeniden üretildi | Android açık/koyu tema, büyük yazı, 6+/11+ satıcı ve ürün, son eksik sayfalar, satır dokunuşu ile yatay swipe ayrımı, geri/handle/backdrop kapatma ve sayfa konumunun korunması bekleniyor | `E0` | Jest gerçek parmak hareketi, üst sekme gesture arbitration'ı, native modal geçişi ve küçük ekrandaki optik yüksekliği kanıtlamaz |
| `SPK-ANA-007` | Kullanıcının kaydettiği etkin ödeme planı Analiz abonelik kartında kaybolmamalı; yoğun en yüksek işlem ve sessiz harcama listeleri ana akışı uzatmamalı | Onaylı aktif planlar ile otomatik tespitler Analiz için birleştirilir; aynı vendor'da onaylı plan önceliklidir ve tutarsız çift kayıt engellenir. En yüksek işlemler ilk 10 sonuçla beşerli iki; sessiz harcamalar ilk 15 sonuçla beşerli en fazla üç sabit yatay sayfadır. İç jest üst sekmeyi kilitler | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/utils/subscriptionAnalytics.ts`, `src/components/analytics/{SubscriptionsCard,TopTxCard,SilentSpendCard,analyticsStyles}.tsx` ve ilgili testler | `AI-2026-08-21-ANALYTICS-PAGERS-SUBSCRIPTIONS-001`: typecheck, odaklı ve tam Jest sonucu katkı kaydında | Android APK'da manuel plan kaydet/düzenle/duraklat; aynı vendor'da detected+confirmed tekilleştirme; 6/10 işlem ve 6/11/15 sessiz kalemle swipe, son eksik sayfa ve üst sekme gesture kontrolü bekleniyor | `E0` | Jest gerçek SQLite yaşam döngüsünü, native pager gesture önceliğini ve optik sabit yüksekliği tek başına kanıtlamaz |
| `SPK-ANA-003` | Sıfır harcama, seri ve hedef-altı istatistikleri kısmi gün veya yapay hedef nedeniyle yanıltıcı olmamalı | Yalnız tamamlanmış günler sayılır; bugün payda ve gün sınıflarından çıkarılır, bugünkü harcama aktif seriyi keser. Takip-temelli uzun dönem ilk gerçek kayıtta başlar; geçmiş aralık serisi dönem sonunda ölçülür. Sabit günlük hedef yalnız aktif kanonik aylık bütçe döngüsünde kullanılır | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/utils/spendingStats.ts`, `src/components/analytics/StreakCard.tsx`, `src/components/StreakDetailsSheet.tsx` | `AI-2026-08-02-SPENDING-STATS-001`: typecheck exit 0; tam Jest 41/41 suite ve 287/287 test; Android Metro export 1.921 modül | Aylık/yıllık/geçmiş aralık geçişleri, bugün işlemli/işlemsiz durum ve görünen metinler hedef cihazda bekleniyor | `E0` | Testler saf takvim ve hesap sözleşmesini doğrular; commit/build kimliği ve cihaz kabulü yoktur |
| `SPK-NOTIF-003` | Uygulama içi bildirimler Android sistem tepsisine açılış flicker'ı, içerik sızıntısı veya çift teslim oluşturmadan taşınmalı | Feed yetkili kalır; native aktivasyon reveal sonrasıdır ve Expo Go native importtan önce guard edilir. Yerelleştirilmiş sessiz `updates` ile yüksek öncelikli `alerts` kanalları `PRIVATE` kilit ekranı kullanır; içeriksiz ledger, kanonik revision kontrolü ve recovery teslimi tekrar güvenli yapar; izin durumu tercihlerden OS ayarına bağlanır | Mevcut çalışma ağacı; `app/{_layout,notifications}.tsx`, `src/context/NotificationsContext.tsx`, `src/services/androidNotificationsSetup.ts`, `src/notifications/{storage,presentation}.ts` | `AI-2026-08-02-ANDROID-SYSTEM-NOTIFICATIONS-001`: typecheck exit 0; tam Jest 41/41 suite ve 287/287 test; Android Metro export 1.921 modül | Fiziksel standalone APK'da cold start, izin, iki kanal, kilit ekranı, resume/yeniden başlatma, silme ve warm/cold tap doğrulaması bekleniyor | `E0` | Expo Go bu native davranışları kanıtlamaz; OS schedule ile SQLite yazımı mutlak atomik değildir ve ani süreç ölümü penceresi cihazda ayrıca sınanmalıdır |
| `SPK-NOTIF-004` | Bir harcama silindikten sonra ona bağlı eski fiş bildirimi feed veya Android panelinde yeniden “işlem kaydedildi” olarak görünmemeli | `receipt-saved-{expenseId}` yalnız mevcut kanonik harcamaya bağlıdır; DB projection'da bulunmayan fiş kartı stale kabul edilip feed'den ve tray'den emekliye ayrılır. Android anlık köprüsü ledger kaydı eksik olsa bile yalnız bu sync'te açıkça oluşturulan veya iki dakikadan taze kayıtları teslim eder; saatler eski okunmamış backlog baseline edilip yeniden uyarılmaz | Mevcut çalışma ağacı; `src/notifications/{receiptNotifications,buildNotifications}.ts`, `src/services/androidNotificationsSetup.ts` ve ilgili testler | `AI-2026-08-13-RECEIPT-DELETE-NOTIFICATION-001`: typecheck exit 0; odaklı 4/4 suite ve 65/65 test; tam Jest 71/71 suite ve 617/617 test; `git diff --check` exit 0 | Standalone APK'da taranmış fişi kaydet → bildirimi gör → saatler eski başka unread kart varken işlemi sil → eski işlem için yeni tray bildirimi oluşmaması ve bağlı kartın feed'den kalkması bekleniyor | `E0` | Jest DB/native köprülerini mock'lar; Android panelinin gerçek cleanup ve yeniden teslim davranışı Faz 6 cihaz kabulünde doğrulanmalıdır |
| `SPK-NOTIF-005` | Tarihi bilinen plan, ödeme, hedef ve bütçe kontrolleri uygulama ekranda değilken de dikkat çekebilmeli; arka planda bilinmeyen harcama varmış gibi davranmamalı | Borç/ödeme rolling horizon'ına hedef `90/30/14/7/3/1/0` gün ve bütçe dönemi `%50/%75/%90` alarmı eklenir. Deterministik desired-state değişiklik/mute'ta eski isteği kaldırır; her ailede yalnız son geçilmiş eşik feed'e bağlanır. Gerçek OS plan sayısı tercihlerde görünür; harcama/kategori eşikleri event-driven kalır | Mevcut çalışma ağacı; ADR-006; `src/notifications/{attentionNativeSchedule,buildNotifications}.ts`, `src/services/{reminderScheduler,androidNotificationsSetup}.ts`, `app/notifications.tsx`, TR/EN/AZ/RU kaynakları | `AI-2026-08-22-CLOSED-APP-ATTENTION-001`: typecheck exit 0; odaklı 6/6 suite ve 72/72 test; tam Jest 90/90 suite ve 720/720 test; locale parity geçti; `git diff --check` exit 0 | Standalone APK: ilk izin, process kill, reboot, plan değişimi/mute iptali, 5–10 dakikalık ödeme alarmı ve gerçek scheduled sayısı; force-stop sınırı ayrı gözlem | `E0` | İlk açılmamış veya Android tarafından zorla durdurulmuş uygulama uyandırılamaz; exact dakika, OEM pil politikası ve yeni finansal veri olmadan harcama eşiği garantisi verilmez |
| `SPK-NOTIF-006` | Uygulama açılışında hesaplanan gecikmiş/bugünkü bildirimler, kapalı uygulamadan zamanında gelmiş Android alarmı gibi görünmemeli; geleceğe planlanan kritik uyarının gerçekten native envantere yazıldığı kullanıcı tarafından denetlenebilmeli | Borç, ödeme planı, hedef ve bütçe ailelerinin geçmiş eşik yakalamaları yalnız uygulama içi feed'de tutulur; eski sürümün ürettiği anlık tepsi kopyaları temizlenirken gerçek `spark:future:v1:` alarm handle'ı korunur. Native desired-state yazımı sistem envanterinden okunarak doğrulanır, eksikler bir kez yeniden denenir; doğrulama veya ledger yazımı başarısızsa bu turdaki native yazımlar geri alınır ve cursor ilerlemez. Bildirim ayarları gerçek doğrulanan/istenen sayı, sonraki tarih, kanal durumu, hata ve onarım eylemi gösterir; borç/plan/hedef/bütçe mutasyonları commit sonrasında best-effort doğrudan senkron olur ve senkron hatası finansal işlemi başarısız göstermeye dönüşmez. Yedek hatırlatması ve çıkarılmış abonelik tahmini uygulama-olayı/feed kapsamındadır | Mevcut çalışma ağacı; ADR-006; `src/services/{androidNotificationsSetup,reminderScheduler}.ts`, `src/context/NotificationsContext.tsx`, `src/notifications/{reminderNotificationFeed,buildNotifications}.ts`, `app/notifications.tsx`, ilgili mutasyon ekranları, dört dil kaynakları ve testler | `AI-2026-08-25-ANDROID-NOTIFICATION-WAKE-HARDENING-001`: typecheck exit 0; bildirim+locale odaklı 9/9 suite ve 131/131 test; mutasyon ekranları 4/4 suite ve 17/17 test; tam Jest 115/115 suite ve 943/943 test; public Expo config doğrulandı; `git diff --check` temiz | Standalone Android APK: izin+kanal açık; 5–10 dakika geleceğe onaylı plan; uygulama sürecini normal kapatma, ekran kilidi/Doze, reboot ve APK update; plan/mute/silme; geçmiş planla uygulama açılışında yeni tepsi bildirimi oluşmaması; eski anlık kopya temizlenirken gerçek future alarmın korunması. Force-stop ayrı ve teslim garantisi dışı | `E0` | Jest Expo API ve envanteri mock'lar; AlarmManager/boot restore, üretici pil kısıtlaması ve gerçek teslim ancak standalone cihazda doğrulanır. Exact-alarm izni bilinçli olarak istenmez; Android/üretici birkaç dakikalık gecikme yapabilir. İlk açılmamış veya kullanıcı tarafından force-stop edilmiş uygulama uyandırılamaz |
| `SPK-REM-001` | Borç vadesi ve kullanıcı tarafından yönetilen düzenli ödeme hatırlatıcıları, nakit-akışı tarihini veya türetilmiş abonelik tahminini bozmadan kalıcı ve yedeklenebilir olmalı | ADR-006: borç `date`/`due_date` ayrımı; tam ödemede atomik reminder kapatma; ayrı kararlı-UID reminder varlığı; saf takvim oluşumu; exact/idempotent backup v3 ilişki haritası. Veri temeli feed veya kapalı uygulama scheduler'ı sayılmaz | Mevcut çalışma ağacı; `src/db/{schema,database,debtDao,recurringPaymentReminderDao}.ts`, `src/utils/recurringSchedule.ts`, `src/services/backupService.ts`, `src/components/BackupSection.tsx` | `AI-2026-08-09-REMINDER-FOUNDATION-001`: odaklı 6/6 suite ve 121/121 test; typecheck exit 0; tam Jest 60/60 suite ve 448/448 test; `git diff --check` exit 0 | Standalone APK'da temiz kurulum, eski DB upgrade, vendor silme-detach ve v1/v2/v3 restore smoke testi bekleniyor; zamanlı OS bildirimi bu fazın kabul kapsamı değildir | `E0` | Değişiklikler henüz commit/CI/build kimliğine bağlı değildir; Jest ve masaüstü SQLite sözleşmesi gerçek Expo SQLite migration yaşam döngüsünü kanıtlamaz |
| `SPK-REM-002` | Kullanıcı yeni veya açık bir borca, işlem tarihini değiştirmeden vade ve hatırlatma tercihi ekleyebilmeli; eski ekran yarışı kapanmış borcu güncelleyememeli | Vade opsiyoneldir; hatırlatma vadesiz açılamaz, vade kaldırılınca kapanır. Oluşturma tek INSERT, düzenleme yalnız açık/bakiyeli borçta tek UPDATE'tir. Liste gelecek/bugün/gecikmiş durumu metin+ikonla sunar; tarih seçici işlem/vade/ödeme hedefini açıkça ayırır. Yüzey native scheduler vaadi vermez | Mevcut çalışma ağacı; `src/components/{DebtSheet,DebtReminderFields,CustomDatePicker}.tsx`, `src/utils/{dateUtils,debtReminder}.ts`, `src/db/debtDao.ts`, dört dil kaynağı | `AI-2026-08-11-DEBT-REMINDER-UX-001`: typecheck exit 0; odaklı 7/7 suite ve 67/67 test; tam Jest 64/64 suite ve 483/483 test; locale parity 11/11; `git diff --check` exit 0 | Standalone Android'de nested sheet/tarih seçici, klavye, geri/drag, açık-koyu tema, büyük font ve TalkBack bekleniyor. Kapalı uygulamaya zamanlı bildirim bu fazın kabulü değildir | `E0` | Component testleri native modal ve gerçek SQLite'ı mock'lar; cihaz kabulü ve Faz 5 scheduler/uzlaştırma tamamlanmadan teslim garantisi yoktur |
| `SPK-REM-003` | Kullanıcı internet/kira gibi düzenli ödemeyi manuel planlayabilmeli veya yerel tahmini açık onayla plana dönüştürebilmeli; tahmin kendiliğinden taahhüt olmamalı | Abonelikler ekranı onaylı planlarla algılanan önerileri ayırır. Plan tek DAO yazısıyla oluşturulur; düzenleme tekrar başlangıcını yalnız program değiştiğinde yeniden sabitler. Pause geri alınabilir, silme onaylıdır ve detected vendor tek plana dönüşür. Add/edit/detected akışları yalnız sayısal kayıt kimliği taşıyan tam ekran route'a gider; başlık sabit, form dikey kaydırılabilir ve kullanıcı finans verisi route parametresine yazılmaz | Mevcut çalışma ağacı; `app/{subscriptions,recurring-payment}.tsx`, `src/components/RecurringPaymentReminderSheet.tsx`, `src/utils/recurringPaymentPlan.ts`, `src/db/recurringPaymentReminderDao.ts`, dört dil kaynağı | Önceki kayıtlar ve `AI-2026-08-21-RECURRING-PAYMENT-FULLSCREEN-001`: typecheck; route+form 2/2 suite ve 8/8 test; tam Jest 86/86 suite ve 692/692 test; `git diff --check` temiz | Standalone Android'de tarih seçici, klavye, sabit başlık, tam dikey kaydırma, sistem geri/gesture, açık-koyu tema, dört dil, büyük font ve create/edit/detected akışı bekleniyor | `E0` | Component/route testleri DAO'yu mock'lar; gerçek SQLite ve native klavye/safe-area davranışı cihaz kabulü olmadan kanıtlanmış sayılmaz |
| `SPK-REM-004` | Kaydedilmiş borç vadeleri ve kullanıcı-onaylı ödeme planları doğru zamanda, tekrar üretmeden ve tahmini abonelik uyarılarıyla karışmadan uygulama içi bildirime dönüşmeli; geçersizleşen kayıtlar feed ve Android tepsisinde bayat kalmamalı | Saf yerel YMD/HH:mm kuralı yaklaşan/bugün/gecikmiş aşamalarını üretir. PII içermeyen deterministik kimlik ve fingerprint silinen aynı uyarının geri gelmesini engeller; teknik feed budaması kullanıcı silmesi sayılmaz ve kapasite baskısında en acil kayıtlar korunur. Uzlaştırma okunma ve ilk oluşma bilgisini korurken bayat aşamaları emekliye ayırır. Borç, onaylı ödeme planı ve tahmin ayrı filtre/mute kanallarıdır; onaylı vendor tahmini bastırır ve mute native retry'a da uygulanır. Feed kanoniktir, geleceğe tarihli native teslim Faz 5'e bırakılır | Mevcut çalışma ağacı; `src/notifications/{channels,reminderNotificationRules,reminderNotificationFeed,buildNotifications,presentation,types}.ts`, `src/context/NotificationsContext.tsx`, `app/{notifications,subscriptions}.tsx`, `src/components/DebtSheet.tsx`, dört dil kaynağı | `AI-2026-08-11-REMINDER-FEED-RULES-001`: typecheck exit 0; tam Jest 69/69 suite ve 547/547 test; locale parity 11/11; `git diff --check` exit 0 | Standalone Android'de uygulama açık/resume, saat sınırı, üç filtre/mute kanalı, settle/pause/reschedule ve emekliye ayrılan Android tepsi kopyalarının temizliği bekleniyor. Kapalı uygulamaya kesin zamanlı teslim Faz 5 kapsamıdır | `E0` | Jest saf kural ve context köprü sözleşmesini doğrular; OS tepsisi, süreç ölümü ve kapalı uygulama teslimi fiziksel APK olmadan kanıtlanmış sayılmaz |
| `SPK-REM-005` | Açık borç ve kullanıcı-onaylı ödeme planı, uygulama kapalıyken de gelecekteki Android alarmı olarak planlanmalı; mute/silme/kapanma/değişiklik eski alarmı bırakmamalı ve uygulama açıldığında aynı uyarı ikinci kez teslim edilmemeli | ADR-006 Faz 5 kararı: saf yerel takvim planlayıcısı borç için yaklaşan+vade günü, ödeme planı için 400 günlük/en çok 14 oluşumlu rolling horizon üretir; global 512 istek adil dağıtılır. Coordinator yalnız SPARK prefix'li gerçek OS isteklerini actual-vs-desired uzlaştırır. Future schedule ile anlık teslim baseline'ı ortak SQLite commit'inde tutulur; hata yeni alarmı telafi iptaliyle geri alır. Pre-reveal sync cursor ilerletemez; cold tap normal bootstrap sync'inden önce işlenir. Gecikmiş pending istek başarıyla iptal edilince baseline temizlenir ve anlık fallback açılır; başarısız iptal kotaya sayılır. Canlı alarm ve fired-cleanup ledger havuzları ayrıdır. DST spring-gap occurrence'ı aynı gün ileri normalize edilir. Fired tray cleanup exact kimlik, içerik özeti ve mute ile doğrulanır; hata halinde retry handle'ı korunur. Exact-alarm izni ve kesin dakika vaadi yoktur | Mevcut çalışma ağacı; `src/notifications/{reminderNativeSchedule,reminderNotificationPresentation,storage}.ts`, `src/services/{reminderScheduler,androidNotificationsSetup}.ts`, `src/context/NotificationsContext.tsx`, `src/db/recurringPaymentReminderDao.ts`, `app/_layout.tsx`, dört dil kaynağı | `AI-2026-08-11-ANDROID-REMINDER-SCHEDULER-001`: typecheck exit 0; odaklı Jest 9/9 suite ve 167/167 test; tam Jest 71/71 suite ve 614/614 test; locale parity odaklı pakete dahil; `git diff --check` exit 0 | Standalone Android APK'da izin, seçilen yerel saat, process-kill, reboot, APK update, Doze/OEM gecikmesi, settle/pause/delete/mute iptali, cold tap, resume-timezone uzlaştırması ve force-stop sınırı bekleniyor | `E0` | Jest Expo API sözleşmesini mock'lar; OS alarmı, boot restore ve gecikme davranışı fiziksel cihaz olmadan kanıtlanmış sayılmaz. Uygulama ledger'ı içeriksizdir ancak Expo/Android özel native deposu sunum metnini saklayabilir |

| `SPK-ANA-007` | Satıcı analizinden açılan ürün ayrıntısı kapatılınca kullanıcı aynı satıcı bağlamına dönmeli; boş kategori sınırı CTA'sı metni çerçeveye yapışmamalı | Satıcı paneli ürün geçişinde silinmek yerine askıya alınır; ürün native kapanışını tamamladıktan sonra aynı satıcı seçimi yeniden gösterilir. Doğrudan karttan açılan ürünler Analiz'e döner. Boş limit CTA'sı yalnız bu yüzeyde yatay iç boşluk, 44 dp minimum hedef ve dar ekranda metin küçülme alanı kullanır | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/components/{ItemAnalyticsModal,analytics/VendorAnalyticsSheet,analytics/LimitsHealthCard}.tsx`, `src/components/analytics/analyticsStyles.ts` | `AI-2026-08-22-ANALYTICS-BACKSTACK-001`: typecheck exit 0; odaklı 3/3 suite ve 11/11 test; tam Jest 90/90 suite ve 721/721 test; `git diff --check` temiz | Android'de satıcı→ürün→geri/X/handle akışı, aynı satıcı paneline dönüş; küçük ekran, büyük font ve dört dilde CTA iç boşluğu bekleniyor | `E0` | Jest native Modal kapanış zamanlamasını, Android sistem geri hareketini ve gerçek cihaz optik boşluğunu tek başına kanıtlamaz |
| `SPK-ANA-008` | Harcama istatistikleri kısa/eksik geçmişten sahte başarı üretmemeli; devam eden dönem karşılaştırması eş günleri kullanmalı; Analiz yalnız anlamlı ve verili kartları göstermeli | Veritabanındaki ilk gerçek harcamadan başlayan kapsam ve en az üç tamamlanmış gün kapısı; kayıtlı gün/payda özeti ve dönem davranışına bağlı mesaj. Harcama karşılaştırması bugün/geleceği dışlar, ilk `N` tamamlanmış günü önceki ilk `N` günle eşler, gerçek aralıkları gösterir ve yükleme hatasını geçerli sıfırdan ayırır. Kayıt saati kartı ile Dashboard bütçe tekrarı kaldırılır; limit/fiyat/abonelik/hedef kartları veri yoksa render edilmez; En Yüksek İşlemler korunur. Tümünü kaldır düzenleme taslağını boşaltabilir; kart işlemleri yalnız onayla yazılır, boş taslak onaylanmaz, düzenleme sırasında ana sekme kaydırması kilitlenir ve sekmeden onaysız ayrılma son kayıtlı yapıyı geri yükler. Eski boş ayar Günlük Grafik ile onarılır | Mevcut çalışma ağacı; `app/(tabs)/analytics.tsx`, `src/db/expenseDao.ts`, `src/utils/{spendingStats,analyticsPeriod,analyticsCardConfig}.ts`, `src/components/{CustomDatePicker,analytics/{StreakCard,MonthlyCompareCard,GoalCard,LimitsHealthCard,SubscriptionsCard}}.tsx`, dört dil kaynağı ve ilgili testler | `AI-2026-08-22-ANALYTICS-TRUST-001`: typecheck başarılı; tam Jest 92/92 suite ve 742/742 test; locale çıktıları üretildi | Android'de aktif/geçmiş/özel dönem; 0/1/2/3+ tamamlanmış gün; gelecek tarih seçimi; sıfır toplam; veri varken/yokken koşullu kartlar; boş onay uyarısı, geri ekleme, düzenlemede yatay kaydırma, sekme düğmesiyle ayrılma ve yeniden açma; açık-koyu tema ve dört dil kontrolü bekleniyor | `E0` | Jest fiziksel tarih seçiciyi, toast yerleşimini, uzun yerelleştirilmiş metni ve gerçek cihazdaki kart/sekme akışını tek başına kanıtlamaz; kayıt olmayan gün, harcama yapılmadığını değil uygulamada o güne ait harcama kaydı bulunmadığını ifade eder |
## 6. Yeni izlenebilirlik kaydı şablonu

Her yeni özellik, hata düzeltmesi veya araştırma için aşağıdaki blok kopyalanır.

### `<KAYIT-KİMLİĞİ>` — `<kısa başlık>`

| Alan | Kayıt |
|---|---|
| Tarih ve saat dilimi | `<YYYY-AA-GG, Europe/Warsaw>` |
| Kaydı açan | `<insan / issue / gözlem>` |
| İnsan tarafından onaylanan kapsam | `<onaylanan davranış ve sınırlar>` |
| Gereksinim | `<ölçülebilir kullanıcı/teknik gereksinim>` |
| Kabul kriterleri | `<Given/When/Then veya madde listesi>` |
| Başlangıç durumu | `<yeniden üretim adımları ve gözlenen sonuç>` |
| Karar/ADR | `<ADR kimliği veya gerekçeli karar>` |
| AI katkısı | `<analiz/kod/test/doküman; araç/model kaydı referansı>` |
| İnsan katkısı | `<gereksinim, seçim, inceleme, manuel test>` |
| Uygulama | `<commit hash ve dosyalar>` |
| Otomatik doğrulama | `<tam komut, exit code, tarih, log/CI URI>` |
| Cihaz doğrulaması | `<build, cihaz, OS, senaryo, sonuç, ekran/video URI>` |
| Nihai insan kabulü | `<kim, tarih, kapsam, referans>` |
| Gizlilik kontrolü | `<temizlendi / redakte edildi / paylaşım kısıtı>` |
| Kalan risk | `<bilinen sınırlama ve takip işi>` |
| Kanıt düzeyi | `<E0–E4>` |
| Retrospektif sınırlama | `<olay sırasında tutulmayan/veri kaybı olan alanlar>` |

## 7. Doğrulama kanıtı biçimi

### Otomatik test kaydı

```text
Commit/build: <hash veya build kimliği>
Tarih/saat dilimi: <ISO-8601>
Ortam: <OS, Node, paket yöneticisi>
Komut: <tam ve yeniden çalıştırılabilir komut>
Exit code: <0/non-zero>
Özet: <suite/test sayısı, typecheck sonucu>
Artefakt: <CI URL veya depo içi log yolu>
Kapsamadığı iddialar: <ör. görsel kalite, gerçek cihaz gesture'ı>
```

### Cihaz doğrulama kaydı

```text
Build: <APK/IPA/EAS build kimliği ve commit>
Cihaz: <üretici/model; kişisel cihaz adı kullanmayın>
OS: <sürüm>
Uygulama modu: <release/development/Expo Go>
Ön koşullar: <tema, dil, DB durumu, ağ>
Adımlar: <numaralı senaryo>
Beklenen: <ölçülebilir sonuç>
Gözlenen: <sonuç>
Artefakt: <redakte edilmiş video/screenshot/log>
İnceleyen ve tarih: <insan>
```

## 8. Gizlilik ve araştırma etiği

- Gemini API anahtarı, token, EAS secret, credential, cihaz UUID'si, mutlak kullanıcı
  yolu ve kişisel finans verisi bu kayda yazılmaz.
- Ekran görüntülerinde bildirim çubuğu, kişi/satıcı isimleri, tutarlar ve dosya
  yolları paylaşım amacına göre redakte edilir.
- Konuşma dökümünün tamamı yerine kararın özeti ve kontrollü referansı tutulur.
- Tez ekine girecek AI çıktıları insan tarafından doğrulanır; hatalı veya reddedilen
  öneriler de seçici biçimde silinmek yerine sonuçlarıyla kaydedilir.
- Hash kullanılıyorsa neyin hash'lendiği, algoritma ve oluşturma tarihi belirtilir;
  hash, içeriğin doğruluğunu değil yalnız bütünlüğünü destekler.

## 9. Retrospektif kayıt sınırlaması

Geçmiş commitlerden sonradan oluşturulan kayıtlar şu riskleri açıkça taşımalıdır:

- O sırada değerlendirilen fakat kayda geçmeyen alternatifler bilinmeyebilir.
- Commit mesajı insan niyetini eksik veya hatalı özetleyebilir.
- Daha sonra güncellenen `DESIGN_BRIEF.md`, olay anındaki bilgiyi yansıtmayabilir.
- Test dosyasının varlığı, testin ilgili committe gerçekten çalıştırıldığını göstermez.
- Bugünkü cihaz sonucu, geçmiş build'in davranışının kanıtı değildir.

Bu nedenle retrospektif satırlar `E0` veya `E1` ile başlatılır; tarihli CI, cihaz ve
insan kabul kanıtı bulunmadıkça daha yüksek düzeye çıkarılmaz.
